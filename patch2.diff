--- packages/backend/convex-modules/base-bindings.ts
+++ packages/backend/convex-modules/base-bindings.ts
@@ -50,6 +50,13 @@
       projectId: args.projectId,
     });

+    // Schedule initial sync push
+    await ctx.scheduler.runAfter(
+      0,
+      internal.baseSyncActions.syncProjectToBase,
+      { bindingId }
+    );
+
     return bindingId;
   },
 });
