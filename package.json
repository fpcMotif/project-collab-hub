{
  "name": "project-collab-hub",
  "private": true,
  "packageManager": "bun@1.3.3",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "turbo run format",
    "format:fix": "turbo run format:fix",
    "check-types": "turbo run check-types",
    "validate": "bun run lint && bun run check-types && bun run build",
    "prepare": "command -v prek >/dev/null 2>&1 && prek install --install-hooks || echo 'Install prek to enable git hooks: https://prek.j178.dev/installation/'"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5.9"
  }
}
