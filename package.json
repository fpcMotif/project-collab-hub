{
  "name": "project-collab-hub",
  "private": true,
  "packageManager": "bun@1.3.3",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "prepare": "command -v prek >/dev/null 2>&1 && prek install && prek install --hook-type pre-push || echo 'Install prek for git hooks: cargo install prek'"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5.9"
  }
}
