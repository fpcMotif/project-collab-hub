{
  "name": "@collab-hub/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check-types": "[ -d convex/_generated ] && tsc --noEmit || echo 'Skipping: run bunx convex dev first to generate types'",
    "dev": "bunx convex dev",
    "test": "vitest run"
  },
  "dependencies": {
    "@collab-hub/feishu-integration": "workspace:*",
    "convex": "^1",
    "effect": "^3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5",
    "vitest": "^4.1.0"
  }
}
