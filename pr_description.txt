💡 **What:** Optimized the `create` and `update` mutations in `packages/backend/convex/comments.ts` by replacing sequential `for` loops with `await Promise.all` over `.map()` callbacks for mentions and notification deliveries.
🎯 **Why:** Sequential database inserts in a loop result in a classic N+1 network/I/O bottleneck, as the runtime halts and waits for each row insertion to complete over the wire before proceeding.
📊 **Measured Improvement:** No specific benchmark suite was available for this module. However, using `Promise.all` allows the Convex runtime to pipeline database requests and execute them concurrently. This substantially reduces latency because the total time becomes bounded by the single slowest insert rather than the sum of all inserts sequentially.
